# Inference-oriented deps (install torch/torchaudio for your CUDA/CPU stack first).
# CosyVoice upstream: https://github.com/FunAudioLLM/CosyVoice/blob/main/requirements.txt
#
# Quick install:
#   conda create -n s3flow python=3.10 -y && conda activate s3flow
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-infer.txt
#   export PYTHONPATH="$(pwd):$(pwd)/third_party/Matcha-TTS"

# --- core audio / model loading ---
HyperPyYAML>=1.2.0
omegaconf>=2.3.0
numpy>=1.24
onnxruntime>=1.16.0
openai-whisper
soundfile
scipy
librosa
pyworld>=0.3.0

# --- model architectures ---
conformer==0.3.2
x-transformers>=2.11.0
diffusers>=0.29.0
transformers>=4.35.0

# --- Matcha-TTS transitive deps (imported during config loading) ---
hydra-core>=1.3.0
lightning>=2.0.0
gdown>=4.6.0

# --- utilities ---
inflect>=7.0.0
wget>=3.2
tqdm>=4.65.0
pandas>=2.0
pyarrow>=14.0

# --- web UI ---
gradio>=4.0
